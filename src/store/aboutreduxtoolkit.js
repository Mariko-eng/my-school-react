// Comes automatically with the following libraries.
//  -redux (core library, state management)
//  -immer (allows to mutate state)
//  -redux-thunk (handles async actions)
//  -reselect (simplifies reducer functions)